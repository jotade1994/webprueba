
function redirectBowl() {
    let phoneNumber = '+59178081193';
    let initialMessage = '¡Hola! Me gustaría pedir el bowl de quinoa.';

    let whatsappURL = `https://wa.me/${phoneNumber}?text=${encodeURIComponent(initialMessage)}`;

    window.open(whatsappURL, '_blank');
}
function redirectWrap() {
    let phoneNumber = '+59178081193';
    let initialMessage = '¡Hola! Me gustaría pedir un wrap de pollo.';

    let whatsappURL = `https://wa.me/${phoneNumber}?text=${encodeURIComponent(initialMessage)}`;

    window.open(whatsappURL, '_blank');
}
function redirectRollito() {
    let phoneNumber = '+59178081193';
    let initialMessage = '¡Hola! Me gustaría pedir los rollitos de verduras en papel de arroz.';

    let whatsappURL = `https://wa.me/${phoneNumber}?text=${encodeURIComponent(initialMessage)}`;

    window.open(whatsappURL, '_blank');
}

function redirectFalafel() {
    let phoneNumber = '+59178081193';
    let initialMessage = '¡Hola! Me gustaría pedir el falafel con ensalada de tabulé.';

    let whatsappURL = `https://wa.me/${phoneNumber}?text=${encodeURIComponent(initialMessage)}`;

    window.open(whatsappURL, '_blank');
}
function redirectTostada() {
    let phoneNumber = '+59178081193';
    let initialMessage = '¡Hola! Me gustaría pedir la tostada de aguacate y huevo pochado.';

    let whatsappURL = `https://wa.me/${phoneNumber}?text=${encodeURIComponent(initialMessage)}`;

    window.open(whatsappURL, '_blank');
}

